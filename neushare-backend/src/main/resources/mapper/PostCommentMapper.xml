<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neushare.mapper.PostCommentMapper">

    <resultMap id="PostCommentVOMap" type="com.neushare.vo.PostCommentVO">
        <id column="id" property="id"/>
        <result column="post_id" property="postId"/>
        <result column="user_id" property="userId"/>
        <result column="content" property="content"/>
        <result column="parent_id" property="parentId"/>
        <result column="deleted" property="deleted"/>
        <result column="create_time" property="createTime"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar_url" property="avatarUrl"/>
    </resultMap>

    <select id="selectCommentsByPostId" resultMap="PostCommentVOMap">
        SELECT pc.*, u.nickname, u.avatar_url
        FROM post_comment pc
        LEFT JOIN user u ON pc.user_id = u.id
        WHERE pc.post_id = #{postId}
        ORDER BY pc.create_time ASC
    </select>

</mapper>
