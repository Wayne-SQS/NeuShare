<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neushare.mapper.CommentLikeMapper">

    <select id="selectByUserIdAndCommentId" resultType="com.neushare.entity.CommentLike">
        SELECT * FROM comment_like WHERE user_id = #{userId} AND comment_id = #{commentId}
    </select>

</mapper>
