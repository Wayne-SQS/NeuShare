<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neushare.mapper.CommentMapper">

    <resultMap id="CommentVOMap" type="com.neushare.vo.CommentVO">
        <id column="id" property="id"/>
        <result column="resource_id" property="resourceId"/>
        <result column="user_id" property="userId"/>
        <result column="content" property="content"/>
        <result column="parent_id" property="parentId"/>
        <result column="create_time" property="createTime"/>
        <result column="username" property="username"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar_url" property="avatarUrl"/>
    </resultMap>

    <select id="selectCommentsByResourceId" resultMap="CommentVOMap">
        SELECT c.*, u.username, u.nickname, u.avatar_url
        FROM comment c
        LEFT JOIN user u ON c.user_id = u.id
        WHERE c.resource_id = #{resourceId}
        ORDER BY c.create_time DESC
    </select>

    <select id="selectCommentsByUserId" resultMap="CommentVOMap">
        SELECT c.*, u.username, u.nickname, u.avatar_url
        FROM comment c
        LEFT JOIN user u ON c.user_id = u.id
        WHERE c.user_id = #{userId}
        ORDER BY c.create_time DESC
    </select>

    <select id="selectCommentVOPage" resultMap="CommentVOMap">
        SELECT c.*, u.username, u.nickname, u.avatar_url
        FROM comment c
        LEFT JOIN user u ON c.user_id = u.id
        ORDER BY c.create_time DESC
    </select>

</mapper>
