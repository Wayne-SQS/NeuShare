# 阶段八：鸿蒙端 API 对齐修复计划

> 日期：2026-06-14
> 状态：待执行

---

## 背景

阶段七审查发现鸿蒙端存在多处 API 返回类型与后端实际响应不匹配的问题，会导致运行时解析失败。经逐项核实后，整理出以下修复计划。

---

## 已完成的修改（本轮已做）

| # | 问题 | 修改 | 验证 |
|---|------|------|------|
| C1 | 缺少 NotificationApi，通知功能不可用 | 创建 `NotificationApi.ets`，5 个端点对齐 NotificationController | 端点/参数/返回类型一致 |
| C2 | ResourceApi.search 返回 `ResourceVO[]`，后端返回分页 | 改为 `ResourceListResult`，补充 pageNum/pageSize/sortBy 参数 | 后端 `GET /api/resource/search` 返回 `PageResult<ResourceVO>` |
| C3 | ResourceApi.getUserResources 返回 `ResourceVO[]`，后端返回分页 | 改为 `ResourceListResult` + 补充分页参数；新增 `checkLiked` 方法 | 后端 `GET /api/resource/user` 返回 `PageResult<ResourceVO>` |
| C4 | FollowApi.check 返回 `boolean`，后端返回 `{isFollowing: boolean}` | 改为 `ApiResponse<{ isFollowing: boolean }>` | 后端 `GET /api/follow/check` 返回 `Result<Map<String, Boolean>>` |
| C4-调用方 | UserProfilePage 解析 `followResp.data as boolean` | 已改为 `followResp.data.isFollowing` | 第 59 行已确认 |
| C2-调用方 | MyResourcesPage `resp.data.map(...)` | 已改为 `resp.data.records.map(...)` | 后端返回分页对象，data.records 才是数组 |
| C3-调用方 | ResourceService.searchResources `resp.data[i]` | 已改为 `resp.data.records[i]` | 同上 |

---

## 待执行修改

### C5. CommentApi.getUserComments 返回类型不匹配

**文件**: `entry/src/main/ets/api/CommentApi.ets` 第 42-44 行

**现状**:
```typescript
async getUserComments(): Promise<ApiResponse<CommentItem[]>> {
    return this.http.get<CommentItem[]>('/api/comment/user');
}
```

**后端实际返回**: `Result<PageResult<CommentVO>>`（分页对象）

**核实**: 该方法在整个项目中**无任何调用点**（grep 确认 0 处调用）

**方案**: 删除该方法（死代码）。如未来需要，再按分页格式重新添加。

**风险**: 无 — 删除无人调用的代码

---

### M1+L5. HttpClient 添加 deleteWithParams 方法

**文件**: `common/src/main/ets/network/HttpClient.ets`

**现状**: HttpClient 有 `postWithParams` 和 `putWithParams`，但无 `deleteWithParams`。需要查询参数的 DELETE 请求只能手动拼接 URL 字符串。

**受影响调用方**:
- `FavoriteApi.remove` 第 46 行: `this.http.del<null>('/api/favorite/remove?resourceId=' + resourceId)`
- `FollowApi.unfollow` 第 25 行: `this.http.del<null>('/api/follow/remove?followedId=' + followedId)`

**方案**:
1. 在 HttpClient 添加 `deleteWithParams<T>(path: string, params: Object)` 方法
2. FavoriteApi.remove 改为 `this.http.deleteWithParams<null>('/api/favorite/remove', { resourceId })`
3. FollowApi.unfollow 改为 `this.http.deleteWithParams<null>('/api/follow/remove', { followedId })`

**风险**: 低 — 新增方法 + 2 处调用方改写，逻辑等价

---

### M2. PageData 接口统一到 common 模块

**文件**:
- `entry/src/main/ets/api/FollowApi.ets` 第 11-14 行: `PageData<T> { records, total }`
- `entry/src/main/ets/api/PostApi.ets` 第 40-46 行: `PageData<T> { current, pages, records, size, total }`

**现状**: 两个文件各自定义了 `PageData`，且字段不同。FollowApi 的版本缺少 `current/pages/size` 字段。

**方案**:
1. 在 `common/src/main/ets/types/PageData.ets` 创建统一的 `PageData<T>` 接口（取 PostApi 的完整版）
2. 在 `common/Index.ets` 导出 `PageData`
3. FollowApi.ets 和 PostApi.ets 删除各自的 `PageData` 定义，改为 `import { PageData } from '@neushare/common'`
4. NotificationApi.ets 也改为引用 common 版本

**风险**: 低 — 纯类型替换，运行时行为不变

---

### M3. FollowListPage userId 死代码清理

**文件**: `entry/src/main/ets/pages/FollowListPage.ets` 第 16、22 行

**现状**:
```typescript
private userId: number = 0;  // 第 16 行
this.userId = params['userId'] as number;  // 第 22 行
```

**核实**:
- `this.userId` 在 FollowListPage 中**无任何 API 调用使用**（API 从 JWT 获取当前用户 ID）
- 调用方 ProfileTab 只传 `{ tab: 0 }` 或 `{ tab: 1 }`，不传 `userId`
- `this.userId` 实际值为 `undefined as number` = `NaN`

**方案**: 删除 `userId` 字段声明和赋值代码

**风险**: 无 — 删除无用代码

---

## 跳过的项

| 问题 | 跳过原因 |
|------|---------|
| M4: PostApi.createPost 传 JSON.stringify 的 tags | 后端 Post 实体 tags 字段类型为 String，存 JSON 字符串是正确行为 |
| M5: PostApi.likePost 空 body POST | HarmonyOS HTTP 库允许无 body 的 POST，后端也不读 body，实际可用 |
| M6: StatsOverview Long→number | 实际数据不可能超过 JS 安全整数范围 (2^53)，无需处理 |
| L2: common 导出但未使用的组件 | GlassCard/GlassNavBar/ColorDot/TypeTag/PressableScale 暂未使用，不影响功能，后续按需清理 |

---

## 修改文件清单

| 文件 | 操作 | 关联 |
|------|------|------|
| `common/src/main/ets/network/HttpClient.ets` | 新增 `deleteWithParams` 方法 | M1 |
| `common/src/main/ets/types/PageData.ets` | 新建统一分页接口 | M2 |
| `common/Index.ets` | 导出 `PageData` | M2 |
| `entry/src/main/ets/api/FollowApi.ets` | 删除本地 PageData → 引用 common；unfollow 改用 deleteWithParams | M1+M2 |
| `entry/src/main/ets/api/PostApi.ets` | 删除本地 PageData → 引用 common | M2 |
| `entry/src/main/ets/api/NotificationApi.ets` | 删除本地分页类型 → 引用 common | M2 |
| `entry/src/main/ets/api/FavoriteApi.ets` | remove 改用 deleteWithParams | M1 |
| `entry/src/main/ets/api/CommentApi.ets` | 删除 getUserComments 死方法 | C5 |
| `entry/src/main/ets/pages/FollowListPage.ets` | 删除 userId 死代码 | M3 |

**总计**: 9 个文件（1 新建 + 8 修改）
