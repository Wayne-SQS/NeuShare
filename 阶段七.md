# 阶段七：优化打磨 — 改动日志

> 日期：2026-06-14

---

## #15 通知单条删除 + 按类型筛选

**状态：✅ 已完成**

### 改动文件

| 文件 | 改动 |
|------|------|
| `neushare-backend/.../controller/NotificationController.java` | 新增 `DELETE /delete/{id}` 端点，带所有者校验；`GET /list` 增加 `@RequestParam(required = false) String type` 参数 |
| `neushare-backend/.../service/NotificationService.java` | 新增 `deleteNotification(Long id, Long userId)` 方法；`getUserNotifications` 签名增加 `String type` 参数 |
| `neushare-backend/.../service/impl/NotificationServiceImpl.java` | 实现 `deleteNotification`（校验 userId 一致后删除）；`getUserNotifications` 传递 type 到 mapper |
| `neushare-backend/.../mapper/NotificationMapper.java` | `selectNotificationPageWithSender` 增加 `@Param("type") String type` |
| `neushare-backend/.../resources/mapper/NotificationMapper.xml` | 新增 `<if test="type != null and type != ''">AND n.type = #{type}</if>` 条件过滤 |

### 说明
- 之前通知只能"全部已读"，无法删除单条；现在支持单条删除（带所有者校验防止越权）
- 通知列表支持按 type 筛选（audit/comment/follow/like/favorite）

---

## #16 收藏列表返回收藏时间

**状态：✅ 已完成**

### 改动文件

| 文件 | 改动 |
|------|------|
| `neushare-backend/.../vo/ResourceVO.java` | 增加 `private LocalDateTime favoriteTime` 字段 |
| `neushare-backend/.../resources/mapper/FavoriteMapper.xml` | SELECT 增加 `f.create_time as favorite_time`；resultMap 增加 `<result column="favorite_time" property="favoriteTime"/>` |

### 说明
- 收藏列表现在返回 `favoriteTime` 字段，前端可展示"收藏于 xxx"

---

## #17 帖子编辑/删除

**状态：✅ 已完成**

### 改动文件

| 文件 | 改动 |
|------|------|
| `entry/.../pages/PostDetailPage.ets` | ① 引入 AuthStore，获取 currentUserId；② 新增编辑/删除按钮（仅帖子作者可见）；③ 新增删除确认弹窗（半透明遮罩 + 确认/取消）；④ `deletePost()` 调用 `communityService.deletePost()` |
| `entry/.../pages/CreatePostPage.ets` | ① 引入 PostApi，新增 `editPostId`/`isEditing` 状态；② `aboutToAppear` 支持从路由参数加载帖子数据（编辑模式）；③ `submitPost` 根据 `isEditing` 调用创建或更新 API；④ 标题和按钮文案动态切换 |

### 说明
- 帖子作者在详情页可看到"编辑"和"删除"按钮
- 编辑跳转 CreatePostPage，自动填充原内容
- 删除需二次确认，防止误操作

---

## #18 用户统计性能优化

**状态：✅ 已完成**

### 改动文件

| 文件 | 改动 |
|------|------|
| `neushare-backend/.../entity/User.java` | 增加 4 个冗余计数字段：`resourceCount`/`followerCount`/`followingCount`/`totalLikesReceived`（Long，@TableField 注解） |
| `neushare-backend/.../controller/UserController.java` | `getUserProfile` 从 User 实体冗余字段直接读取，移除 FollowService/ResourceService 的 COUNT 查询 |
| `neushare-backend/.../task/CounterCalibrationTask.java` | 拆分为 `calibrateResourceCounters()` + `calibrateUserCounters()`；新增用户计数器校准：重新计算 resourceCount/followerCount/followingCount/totalLikesReceived |
| `neushare-backend/.../resources/db/init.sql` | ① `user` 表增加 4 列（`resource_count`/`follower_count`/`following_count`/`total_likes_received`，BIGINT DEFAULT 0）；② 新增 4 条 UPDATE 语句为已有用户填充初始统计数据 |

### 说明
- 之前用户主页每次请求都执行 3-4 次 COUNT 查询，高并发下性能差
- 现在读冗余字段（O(1)），由定时任务每日凌晨3点校准
- 冗余字段 + 定时校准 = 读写分离，牺牲少量实时性换取查询性能

---

## #19 毛玻璃性能分级

**状态：✅ 已完成**

### 改动文件

| 文件 | 改动 |
|------|------|
| `common/.../utils/BlurPerformance.ets` | **新建** — 毛玻璃性能分级工具类；三级模糊策略（NONE=0/LOW=20/HIGH=40）；AppStorage 键 `isScrolling`/`blurLevel`；`effectiveBlur(isDark)` 计算有效模糊半径；`fallbackBg(isDark)` 返回纯色降级背景 |
| `common/.../components/GlassCard.ets` | ① 引入 BlurPerformance；② 新增 `@StorageLink('isScrolling')`/`@StorageLink('blurLevel')`；③ `effectiveBlur` 替代固定 `blurRadius`；④ 模糊关闭时背景色降级为不透明色；⑤ 添加 150ms 过渡动画 |
| `common/.../components/GlassNavBar.ets` | 同 GlassCard 逻辑：引入 BlurPerformance + effectiveBlur + 降级背景 + 过渡动画 |
| `common/Index.ets` | 导出 `BlurPerformance` 和 `BlurLevel` |
| `entry/.../pages/Index.ets` | ① 引入 BlurPerformance；② `aboutToAppear` 中调用 `BlurPerformance.init()` + `detectDeviceCapability()` |
| `entry/.../pages/tabs/HomeTab.ets` | Scroll 组件添加 `.onScrollFrameBegin()` + `.onScrollStop()` 回调 |
| `entry/.../pages/tabs/CategoryTab.ets` | 同上 |
| `entry/.../pages/tabs/ProfileTab.ets` | 同上 |
| `entry/.../pages/CommunityPage.ets` | List 组件添加 `.onScrollFrameBegin()` + `.onScrollStop()` 回调 |

### 说明
- 毛玻璃（backdropBlur）是 GPU 密集操作，滚动时持续计算会导致掉帧
- 现在滚动时自动关闭 blur，背景切换为不透明纯色（150ms 平滑过渡）
- 深色模式本身不用毛玻璃（设计语言为极简黑白），`effectiveBlur` 直接返回 0
- 低端设备可设置 `blurLevel=LOW` 降级

---

## #20 深色模式组件适配

**状态：✅ 已完成**

### 改动文件

**核心新增：**

| 文件 | 改动 |
|------|------|
| `common/.../theme/ColorTokens.ets` | 新增 `textOnPrimary(isDark)` 方法：亮色返回白字、深色返回深色字（因深色品牌色为纯白） |
| `common/.../components/ColorDot.ets` | 深色模式星空效果：纯白圆点 + 随机闪烁动画（2-3s 周期，EaseInOut，无限循环） |

**硬编码修复（60+ 处）：**

| 替换模式 | 涉及文件数 | 说明 |
|---------|-----------|------|
| `'#0E0E10'` → `ColorTokens.textOnPrimary()` | 20+ | 品牌色背景上的文字色（按钮、标签、选中项等） |
| `ColorTokens.RED` → `ColorTokens.error()` | 6 | 错误/删除文字色适配深色 |
| `ColorTokens.GREEN` → `ColorTokens.success()` | 5 | 成功/确认文字色适配深色 |
| `ColorTokens.TEXT_DISABLED` → `ColorTokens.textDisabled()` | 8 | 禁用态文字色适配深色 |
| `ColorTokens.BG_AVATAR` → `ColorTokens.resolve(BG_AVATAR, BG_AVATAR_DARK)` | 5 | 头像占位底色适配深色 |
| `ColorTokens.WARNING_LIGHT` → `ColorTokens.warning()` | 2 | 警告色适配深色 |

**修复的页面/组件清单：**

| 文件 | 修复内容 |
|------|---------|
| `pages/Index.ets` | 浮动按钮、侧边栏按钮文字 |
| `pages/tabs/HomeTab.ets` | 推荐帖子标题、副标题、年级标签 |
| `pages/tabs/ProfileTab.ets` | 主题切换、Tab 选中、排序芯片、头像底色、退出登录 |
| `pages/tabs/SearchTab.ets` | 禁用态文字 |
| `pages/DetailPage.ets` | 重试按钮、标签文字、禁用态 |
| `pages/PostDetailPage.ets` | 编辑按钮、推荐标签、发送按钮、删除确认遮罩 |
| `pages/LoginPage.ets` | Logo、登录按钮、错误提示、跳过登录 |
| `pages/RegisterPage.ets` | 学院/年级选中、注册按钮、错误提示 |
| `pages/UploadPage.ets` | 分类/类型选中、提交按钮 |
| `pages/SearchPage.ets` | 热门标签、结果标签、排序芯片、禁用态 |
| `pages/CommunityPage.ets` | 发帖按钮、标签筛选、帖子标签、错误/禁用/警告色 |
| `pages/CreatePostPage.ets` | 提交按钮、错误/禁用色 |
| `pages/EditProfilePage.ets` | 保存按钮、头像底色 |
| `pages/CategoryPage.ets` | 标签文字 |
| `pages/PostgradGuidePage.ets` | 科目标签、Tip 文字、备考阶段、CTA 按钮、成功/错误色 |
| `pages/StatsPage.ets` | 标签文字 |
| `pages/WebViewPage.ets` | 标签文字 |
| `pages/SplashPage.ets` | 标签文字 |
| `pages/IdentityCardPage.ets` | 标签文字、成功/错误/禁用色、头像底色 |
| `pages/MyResourcesPage.ets` | 标签文字、错误/禁用色 |
| `pages/ProfilePage.ets` | 错误/成功/禁用色、头像底色 |
| `pages/UserProfilePage.ets` | 头像底色 |
| `pages/FollowListPage.ets` | 头像底色 |
| `components/ResourceCard.ets` | 标签文字 |
| `components/ModelCompare.ets` | 标签文字、成功/错误/禁用色 |
| `components/GradePill.ets` | 标签文字 |

### 说明
- 最严重的问题是 `'#0E0E10'` 硬编码——深色模式下品牌色为纯白，深色文字在白色背景上不可见
- `textOnPrimary()` 方法统一处理"品牌色背景上的文字"场景：亮色白字、深色深字
- `ColorTokens.RED`/`GREEN`/`TEXT_DISABLED` 等静态常量在深色模式下不会自动切换，全部替换为对应的 `isDark` 动态方法
- ColorDot 深色模式从静态纯白圆点升级为星空闪烁效果

---

## 文件统计

| 类别 | 数量 |
|------|------|
| 后端修改 | 8 个 |
| 后端新建 | 0 个 |
| 鸿蒙 common 修改 | 4 个 |
| 鸿蒙 common 新建 | 1 个（BlurPerformance.ets） |
| 鸿蒙 entry 修改 | 22 个 |
| 鸿蒙 entry 新建 | 0 个 |
| **总计** | **35 个文件** |
