# NeuShare 后期开发维护问题清单

> 生成日期：2026-06-07
> 基于项目全面审查后的业务逻辑缺口、安全隐患、数据一致性问题和体验改进建议

---

## 一、安全问题（P0 - 必须修）

### 1.1 资源删除无所有者校验

- **现状**：`DELETE /api/resource/delete/{id}` 未校验当前用户是否为资源所有者，任何登录用户可删除任意资源
- **影响**：用户互删，数据丢失
- **位置**：`ResourceController.java` → `deleteResource()`
- **修复方向**：删除前校验 `resource.getUploadUserId().equals(userId)` 或 `role.equals("admin")`

### 1.2 资源更新无所有者校验

- **现状**：`PUT /api/resource/update` 同样未校验所有者
- **影响**：篡改他人资源
- **位置**：`ResourceController.java` → `updateResource()`
- **修复方向**：同上，校验所有者或管理员

### 1.3 点赞无防刷机制

- **现状**：没有 `resource_like` 表，只做 `like_count` 增减，无法记录谁点赞了什么
- **影响**：
  - 可无限刷赞
  - 刷新后点赞状态丢失（前端 `isLiked` 重置为 false）
  - 无法做"点赞列表"
- **修复方向**：新增 `resource_like` 表 `(user_id, resource_id, create_time)`，唯一约束防重复

### 1.4 数据库密码明文

- **现状**：`application.yml` 中 `password: SQS626070` 直接写在配置文件中
- **修复方向**：使用环境变量 `${DB_PASSWORD}` 或密钥管理服务

### 1.5 JWT 密钥明文且偏短

- **现状**：密钥 `neushare2024secretkeyforjwttoken123456789` 仅 38 字节
- **修复方向**：使用环境变量注入，密钥长度建议 256 位以上

### 1.6 CORS 允许所有来源

- **现状**：`addAllowedOriginPattern("*")` + `allowCredentials(true)` 组合
- **影响**：生产环境有 CSRF 风险
- **修复方向**：限制为实际域名

---

## 二、数据一致性问题（P1）

### 2.1 关注表缺唯一约束

- **现状**：`follow` 表 `(follower_id, followed_id)` 无唯一索引
- **影响**：并发下可重复关注
- **修复方向**：`ALTER TABLE follow ADD UNIQUE KEY uk_follow (follower_id, followed_id);`

### 2.2 删除级联缺失

- **现状**：所有关联关系都没有 `ON DELETE CASCADE`，也没有 Service 层级联删除
- **影响**：
  - 删除用户 → 其资源/评论/收藏成为孤儿数据
  - 删除资源 → 其评论/收藏不清理
- **修复方向**：至少在 Service 层做级联删除

### 2.3 逻辑删除配置无效

- **现状**：`application.yml` 配置了 `logic-delete-field: deleted`，但实体类和表中都没有该字段（comment 表已加，其他表未加）
- **影响**：当前除评论外所有删除都是物理删除，不可恢复
- **修复方向**：统一决策——要么全部加 `deleted` 字段启用逻辑删除，要么移除 yml 配置

### 2.4 冗余计数器无校准机制

- **现状**：`resource` 表的 `view_count / like_count / favorite_count` 是冗余字段
- **影响**：事务失败回滚时计数器可能与实际不一致
- **修复方向**：加定时任务定期 `SELECT COUNT(*)` 校准

---

## 三、业务逻辑缺口（P1-P2）

### 3.1 通知系统缺失（P2）

- **现状**：用户没有任何通知渠道
- **缺失场景**：
  - 资源审核通过/被驳回 → 上传者不知道
  - 评论被回复 → 被回复者不知道
  - 被关注 → 被关注者不知道
  - 资源被收藏/点赞 → 上传者不知道
- **修复方向**：新增 `notification` 表 + 鸿蒙端 Push Kit 推送

### 3.2 审核驳回无原因（P1）

- **现状**：审核只有通过/驳回，驳回时没有原因字段
- **影响**：用户不知道为什么被驳回，无法修改后重新提交
- **修复方向**：`resource` 表加 `reject_reason` 字段，审核驳回时必填

### 3.3 搜索功能弱（P2）

- **现状**：
  - 后端搜索只支持 `keyword` 模糊匹配标题和描述，没有分词
  - 没有搜索历史
  - 没有搜索建议/自动补全
  - 没有按时间/热度/相关度排序（前端排序 UI 已有但后端不支持）
- **修复方向**：至少加排序参数 `sortBy=hot|new|relevant`

### 3.4 用户资料不完整（P2）

- **缺失**：
  - `UserVO` 没有 `email`、`studentId` 字段
  - 没有头像上传接口（只能填 URL）
  - 没有修改用户名功能
  - 注册时没有学院/年级选择

### 3.5 分类体系太扁平（P3）

- **现状**：只有一级分类，没有父子层级
- **影响**：无法按学院→专业→课程逐级筛选
- **备注**：鸿蒙端 `courseData.js` 有完整的学院/专业/课程数据，但后端没有对应结构

---

## 四、体验类改进（P2-P3）

### 4.1 鸿蒙端离线体验可更好（P3）

- **现状**：离线只能看本地 JSON 数据，无法看之前浏览过的资源详情
- **改进**：浏览过的资源详情缓存到本地 Preferences/SQLite

### 4.2 上传体验不完整（P2）

- **缺失**：
  - 只能填 URL，不能直接上传文件
  - 没有上传进度显示
  - 没有草稿保存功能
- **备注**：`FileUploadUtil` 已存在但鸿蒙端未对接

### 4.3 社区功能是"孤岛"（P3）

- **现状**：鸿蒙端 `CommunityService` 是纯本地存储，和后端无关；Web 端没有社区功能
- **影响**：帖子无法跨设备同步

### 4.4 管理后台功能薄弱（P3）

- **缺失**：
  - Dashboard 图表用硬编码数据
  - 没有资源编辑功能（只能审核/删除）
  - 没有公告/通知管理
  - 没有数据导出功能

---

## 五、代码质量问题（P3）

### 5.1 Md5Util 命名误导

- **现状**：类名 `Md5Util`，实际使用 BCrypt
- **修复方向**：重命名为 `PasswordUtil` 或 `BCryptUtil`

### 5.2 CLAUDE.md 版本信息过时

- **现状**：记录 Spring Boot 为 2.7.18，实际为 3.3.7
- **影响**：新开发者可能按旧版 API 编写代码
- **修复方向**：更新 CLAUDE.md 中的技术栈版本

### 5.3 comment 表缺 update_time

- **现状**：评论没有更新时间字段
- **影响**：软删除后无法知道删除时间
- **修复方向**：`ALTER TABLE comment ADD COLUMN update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP;`

---

## 六、优先级总览

| 优先级 | 事项 | 类型 |
|--------|------|------|
| **P0** | 资源删除加所有者校验 | 安全 |
| **P0** | 资源更新加所有者校验 | 安全 |
| **P0** | 点赞表 + 防重复 | 安全/功能 |
| **P1** | follow 表加唯一约束 | 数据一致性 |
| **P1** | 审核驳回原因 | 业务逻辑 |
| **P1** | 删除级联处理 | 数据一致性 |
| **P2** | 通知系统 | 业务逻辑 |
| **P2** | 搜索排序 | 业务逻辑 |
| **P2** | 文件上传对接 | 体验 |
| **P2** | 数据库密码环境变量化 | 安全 |
| **P3** | 离线缓存详情 | 体验 |
| **P3** | 社区后端化 | 功能对齐 |
| **P3** | 分类层级化 | 业务逻辑 |
| **P3** | Md5Util 重命名 | 代码质量 |
| **P3** | CLAUDE.md 更新 | 文档 |
