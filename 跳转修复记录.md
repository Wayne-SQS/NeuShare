# 跳转不对应问题修复记录

> 日期：2026-06-12
> 修复范围：Web 前端、鸿蒙端、后端三端跳转/数据对应问题

---

## 修改文件清单

| # | 文件路径 | 修改内容 | 严重度 |
|---|---------|---------|--------|
| 1 | `neushare-frontend/src/router/index.js` | 新增 `/upload/:id` 路由（编辑资料功能） | 严重 |
| 2 | `entry/src/main/resources/base/profile/main_pages.json` | 注册 `UploadPage` 和 `MyResourcesPage` 页面路由 | 严重 |
| 3 | `entry/src/main/ets/models/ResourceModel.ets` | `adaptBanner` 区分 categoryId（纯数字）和外部 URL，不再硬编码 `linkType: 'category'` | 严重 |
| 4 | `entry/src/main/ets/pages/tabs/HomeTab.ets` | 1) Banner 点击增加 `external` 类型处理（跳转搜索页）+ `isNaN` 防护；2) SearchBar `onSearch` 传递 keyword 参数；3) 推荐帖子跳转传递 `highlightPostId` | 严重+中等 |
| 5 | `entry/src/main/ets/pages/tabs/SearchTab.ets` | SearchBar `onSearch` 传递 keyword 参数 | 中等 |
| 6 | `entry/src/main/ets/data/banners.json` | Banner id=1 linkValue 从 `"4"`（教学软件）改为 `"3"`（开源资源），匹配"大模型编程教程"主题 | 中等 |
| 7 | `neushare-backend/src/main/resources/mapper/FavoriteMapper.xml` | `selectUserFavorites` 改为明确列名（避免 `r.*` 多表 JOIN 歧义）+ 加 `r.id IS NOT NULL` 过滤已删除资源 | 中等 |
| 8 | `neushare-backend/src/main/java/com/neushare/controller/ResourceController.java` | `getResourceDetail` 增加 status 权限校验：未发布资源仅上传者和管理员可查看，其他用户返回 404 | 中等 |
| 9 | `neushare-frontend/src/views/Home.vue` | 轮播图增加 `@click` 跳转 + `localImages` 增加 `link` 属性 | 中等 |

---

## 详细修复说明

### #1：Web 前端 `/upload/:id` 路由缺失（严重）

**问题**：`MyResources.vue` 中点击"编辑"按钮执行 `router.push('/upload/${row.id}')`，但路由只定义了 `/upload`，没有 `/upload/:id`，导致跳转到 404 页面。

**修复**：在 `router/index.js` 中新增 `/upload/:id` 路由，指向同一个 `Upload.vue` 组件（该组件已支持通过 `route.params.id` 判断编辑模式）。

### #2-3：鸿蒙端 UploadPage/MyResourcesPage 未注册路由（严重）

**问题**：`Index.ets` 和 `ProfileTab.ets` 中通过 `router.pushUrl` 跳转到 `pages/UploadPage` 和 `pages/MyResourcesPage`，但这两个页面未在 `main_pages.json` 中注册，运行时路由失败。

**修复**：在 `main_pages.json` 的 `src` 数组中添加 `"pages/UploadPage"` 和 `"pages/MyResourcesPage"`。

### #4：鸿蒙端 adaptBanner 将 URL 当 categoryId（严重）

**问题**：`adaptBanner` 将后端 `BannerVO.linkUrl`（可能是 HTTP URL）直接赋值给 `linkValue`，且硬编码 `linkType: 'category'`。HomeTab 中对 category 类型执行 `parseInt(linkValue)`，若后端返回 URL 则得到 `NaN`，跳转到无效分类。

**修复**：
- `adaptBanner` 中用 `/^\d+$/` 判断 `linkUrl` 是否为纯数字，分别设置 `linkType: 'category'` 或 `'external'`
- HomeTab 的 `onBannerTap` 增加 `external` 类型处理（跳转搜索页用标题搜索）+ `isNaN` 防护

### #5：鸿蒙端 banners.json 分类指向错误（中等）

**问题**：Banner id=1 标题为"大模型编程教程全面上线"，`linkValue="4"` 指向 categoryId=4（教学软件），但大模型编程教程资源实际属于 categoryId=3（开源资源）。

**修复**：将 `linkValue` 从 `"4"` 改为 `"3"`。

### #6：鸿蒙端 SearchBar keyword 丢失（中等）

**问题**：`HomeTab` 和 `SearchTab` 中 SearchBar 的 `onSearch` 回调忽略了 `keyword` 参数，跳转 SearchPage 时未传递关键词，用户输入丢失。

**修复**：`onSearch: (keyword: string) => { router.pushUrl({ url: 'pages/SearchPage', params: { keyword } }); }`

### #7：鸿蒙端推荐帖子缺 highlightPostId（轻微）

**问题**：推荐帖子卡片点击跳转 CommunityPage 时未传递 `highlightPostId`，无法定位到被推荐的帖子。

**修复**：跳转时传递 `params: { highlightPostId: this.recommendedPost?.id || '' }`。

### #8：后端收藏列表未过滤已删除资源（中等）

**问题**：`FavoriteMapper.xml` 中 `selectUserFavorites` 使用 `r.*`（4 表 JOIN 时 id 列歧义），且未过滤已删除资源（资源被物理删除后 LEFT JOIN 返回全 null）。

**修复**：
- 改为明确列出所有字段名，避免 `r.*` 歧义
- 添加 `AND r.id IS NOT NULL` 过滤已删除资源

### #9：后端资源详情公开接口可访问未发布资源（中等）

**问题**：`GET /api/resource/detail/{id}` 是公开接口，无 status 过滤，任何人可通过猜测 ID 查看待审核或已驳回资源。

**修复**：在 Controller 层增加 status 校验——已发布资源(status=1)正常返回，未发布资源仅上传者和管理员可查看，其他用户返回 404。

### #10：Web 前端轮播图点击跳转（中等）

**问题**：Home.vue 轮播图使用硬编码本地图片，未绑定任何点击跳转事件，后端 banner 数据中的 linkUrl 无法被使用。

**修复**：
- 轮播图 div 增加 `@click="item.link && $router.push(item.link)"`
- `localImages` 数组增加 `link` 属性（与后端 init.sql 中 banner 的 linkUrl 对应）
