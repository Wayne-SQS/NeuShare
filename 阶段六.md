# 阶段六：功能补全 — 改动日志

> 日期：2026-06-13

---

## #6 /api/resource/list 加 sortBy 参数

**状态：✅ 已完成**

### 改动文件

| 文件 | 改动 |
|------|------|
| `neushare-backend/.../controller/ResourceController.java` | `/list` 接口增加 `@RequestParam(defaultValue = "new") String sortBy` 参数 |
| `neushare-backend/.../service/ResourceService.java` | `getResourcePage` 方法签名增加 `String sortBy` 参数 |
| `neushare-backend/.../service/impl/ResourceServiceImpl.java` | `getResourcePage` 实现增加 `sortBy` 参数，传递给 mapper |
| `neushare-backend/.../mapper/ResourceMapper.java` | `selectResourcePage` 方法签名增加 `@Param("sortBy") String sortBy` |
| `neushare-backend/.../resources/mapper/ResourceMapper.xml` | `selectResourcePage` SQL 中 `ORDER BY r.create_time DESC` 替换为 `<choose>` 动态排序：`sortBy=hot` 时按 `view_count DESC, like_count DESC`，否则按 `create_time DESC` |

### 说明
- `sortBy=hot` 按浏览量+点赞排序，`sortBy=new`（默认）按时间倒序
- `/api/resource/search` 接口已有此功能，现在 `/api/resource/list` 也支持了

---

## #7 社区功能对接后端

**状态：✅ 已完成**

### 后端改动

| 文件 | 改动 |
|------|------|
| `neushare-backend/.../resources/db/init.sql` | 新增 4 张表：`post`（帖子主表，含 JSON 字段 tags/imageUrls/files）、`post_like`（防重复点赞）、`post_favorite`（防重复收藏）、`post_comment`（树形评论+软删除）；新增 5 条示例帖子数据 |
| `neushare-backend/.../entity/Post.java` | **新建** — 帖子实体，字段：id/userId/title/content/tags/imageUrls/files/isRecommended/viewCount/likeCount/favoriteCount/commentCount/status/createTime/updateTime |
| `neushare-backend/.../entity/PostLike.java` | **新建** — 帖子点赞记录，唯一约束 `uk_user_post(user_id, post_id)` |
| `neushare-backend/.../entity/PostFavorite.java` | **新建** — 帖子收藏记录，唯一约束 `uk_user_post(user_id, post_id)` |
| `neushare-backend/.../entity/PostComment.java` | **新建** — 帖子评论，含 parentId 树形结构 + deleted 软删除 |
| `neushare-backend/.../vo/PostVO.java` | **新建** — 帖子视图对象，含作者信息 authorNickname/authorAvatarUrl/authorCollege |
| `neushare-backend/.../vo/PostCommentVO.java` | **新建** — 评论视图对象，含用户信息 + children 树形结构 |
| `neushare-backend/.../mapper/PostMapper.java` | **新建** — 分页查询（支持 status/tag/keyword/sortBy）、详情、热门、用户帖子 |
| `neushare-backend/.../mapper/PostLikeMapper.java` | **新建** — selectByUserIdAndPostId |
| `neushare-backend/.../mapper/PostFavoriteMapper.java` | **新建** — selectByUserIdAndPostId |
| `neushare-backend/.../mapper/PostCommentMapper.java` | **新建** — selectCommentsByPostId（JOIN user 获取评论者信息） |
| `neushare-backend/.../resources/mapper/PostMapper.xml` | **新建** — 含 JSON_CONTAINS 标签过滤、hot/new 排序、LEFT JOIN user 表 |
| `neushare-backend/.../resources/mapper/PostLikeMapper.xml` | **新建** |
| `neushare-backend/.../resources/mapper/PostFavoriteMapper.xml` | **新建** |
| `neushare-backend/.../resources/mapper/PostCommentMapper.xml` | **新建** — 含 PostCommentVOMap + JOIN user 查询 |
| `neushare-backend/.../service/PostService.java` | **新建** — 接口：分页/详情/CRUD/点赞/收藏/推荐 |
| `neushare-backend/.../service/PostCommentService.java` | **新建** — 接口：评论树/添加/删除 |
| `neushare-backend/.../service/impl/PostServiceImpl.java` | **新建** — 完整实现：原子更新计数器、防重复点赞/收藏、级联删除、通知推送、管理员推荐（互斥） |
| `neushare-backend/.../service/impl/PostCommentServiceImpl.java` | **新建** — 树形构建、软删除、评论数原子更新、通知 |
| `neushare-backend/.../controller/PostController.java` | **新建** — `/api/post/*` 全部端点（list/detail/hot/create/update/delete/like/favorite/recommend/user） |
| `neushare-backend/.../controller/PostCommentController.java` | **新建** — `/api/post/comment/*` 评论端点（list/add/delete），add 用 @RequestParam 遵循项目约定 |
| `neushare-backend/.../config/WebMvcConfig.java` | 新增 4 个公开路径：`/api/post/list`、`/api/post/hot`、`/api/post/detail/**`、`/api/post/comment/list/**` |

### 鸿蒙端改动

| 文件 | 改动 |
|------|------|
| `entry/.../api/PostApi.ets` | **新建** — 社区帖子后端 API 封装层，含 PostVO/PostCommentVO/PageData 接口定义，以及全部 API 方法（列表/详情/创建/点赞/收藏/评论等） |
| `entry/.../models/PostModel.ets` | 重写 — Post 接口字段从本地存储格式更新为后端对接格式（id:number, authorNickname, likeCount, favoriteCount, commentCount, isRecommended, isLiked, isFavorited 等）；新增 PostComment 接口和 toPost()/toComment() 转换函数 |
| `entry/.../services/CommunityService.ets` | 重写 — 从本地 PreferencesStore 存储改为调用 PostApi 后端接口；新增分页支持；点赞/收藏改为 toggle 逻辑 |
| `entry/.../pages/CommunityPage.ets` | 重写 — 移除 Context 依赖和本地存储调用；postId 类型从 string 改为 number；新增分页支持；点赞/收藏根据 isLiked/isFavorited 状态 toggle |
| `entry/.../pages/PostDetailPage.ets` | 重写 — 新增完整评论系统（评论列表/发表评论/回复评论/删除评论）；使用 PostApi 获取数据和交互；点赞/收藏状态持久化 |
| `entry/.../pages/CreatePostPage.ets` | 重写 — 移除 author 输入框和 Context 依赖；提交改为调用后端 API |
| `entry/.../pages/tabs/HomeTab.ets` | 字段名更新：`author→authorNickname`、`createdAt→createTime` |
| `entry/.../pages/Index.ets` | 字段名更新：banner subtitle 中 `author→authorNickname`、`likes→likeCount`、`favorites→favoriteCount`、`commentsCount→commentCount`；`p.id→p.id.toString()` |

### API 清单

**公开（无需登录）：**
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/post/list | 帖子分页 `?pageNum&pageSize&tag&keyword&sortBy` |
| GET | /api/post/detail/{id} | 帖子详情（自动浏览量+1） |
| GET | /api/post/hot | 热门帖子 `?limit` |
| GET | /api/post/comment/list/{postId} | 帖子评论列表（树形） |

**需登录：**
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/post/create | 创建帖子（JSON body） |
| PUT | /api/post/update | 更新帖子（JSON body） |
| DELETE | /api/post/delete/{id} | 删除帖子 |
| POST | /api/post/like/{id} | 点赞 |
| DELETE | /api/post/like/{id} | 取消点赞 |
| GET | /api/post/like/check/{id} | 检查是否已点赞 |
| POST | /api/post/favorite/{id} | 收藏 |
| DELETE | /api/post/favorite/{id} | 取消收藏 |
| GET | /api/post/favorite/check/{id} | 检查是否已收藏 |
| GET | /api/post/user | 我的帖子 |
| PUT | /api/post/recommend/{id} | 推荐帖子（管理员） |
| POST | /api/post/comment/add | 添加评论 `params: postId, content, parentId?` |
| DELETE | /api/post/comment/delete/{id} | 删除评论 |

---

## #8 UserProfilePage 调 API

**状态：✅ 已完成**

### 改动文件

| 文件 | 改动 |
|------|------|
| `entry/.../api/UserApi.ets` | **新建** — 用户 API 模块，提供 `getProfile(userId)` 调用 `GET /api/user/{id}` |
| `entry/.../pages/UserProfilePage.ets` | ① 新增 `resourceCount`/`followerCount`/`followingCount`/`totalLikesReceived` 状态变量；② 引入 `UserApi`，`aboutToAppear` 中先展示路由参数再调 API 获取完整资料；③ 关注按钮下方新增四列统计信息（资源/粉丝/关注/获赞） |

### 说明
- 之前用户资料页只从路由参数获取基本信息，现在会调用后端 API 获取完整资料（含统计数）
- 路由参数作为即时展示的 fallback，API 返回后覆盖更新

---

## #9 ProfileTab 加粉丝/关注数

**状态：✅ 已完成**

### 改动文件

| 文件 | 改动 |
|------|------|
| `entry/.../pages/tabs/ProfileTab.ets` | ① 新增 `followerCount`/`followingCount` 状态变量；② 引入 `UserApi`，新增 `loadFollowStats()` 方法；③ 在 `aboutToAppear`/`onPageShow` 中调用；④ 统计行新增"粉丝"/"关注"项，点击跳转 `FollowListPage` |

---

## #10 关注/粉丝列表页

**状态：✅ 已完成**

### 改动文件

| 文件 | 改动 |
|------|------|
| `entry/.../pages/FollowListPage.ets` | **新建** — 关注/粉丝列表页，支持 Tab 切换、分页加载、呼吸动画空状态、点击跳转用户主页 |
| `entry/.../api/FollowApi.ets` | ① `FollowUser` 接口新增 `college` 字段；② 新增 `PageData<T>` 接口；③ `getFollowing`/`getFollowers` 方法改为支持分页参数 |
| `entry/.../resources/base/profile/main_pages.json` | 路由列表添加 `"pages/FollowListPage"` |

---

## #11 UserProfileVO 加 totalLikesReceived

**状态：✅ 已完成**

### 改动文件

| 文件 | 改动 |
|------|------|
| `neushare-backend/.../vo/UserProfileVO.java` | 增加 `private Long totalLikesReceived` 字段 |
| `neushare-backend/.../service/ResourceService.java` | 增加 `Long getTotalLikesReceived(Long userId)` 方法声明 |
| `neushare-backend/.../service/impl/ResourceServiceImpl.java` | 实现 `getTotalLikesReceived`：查询用户所有已发布资源的 `like_count` 之和 |
| `neushare-backend/.../controller/UserController.java` | `getUserProfile` 方法中添加 `vo.setTotalLikesReceived(resourceService.getTotalLikesReceived(id))` |

---

## #12 Web 端点赞状态持久化

**状态：✅ 已完成**

### 改动文件

| 文件 | 改动 |
|------|------|
| `neushare-frontend/src/views/ResourceDetail.vue` | ① import 行添加 `checkLiked`；② `fetchResource` 函数中资源加载成功后调用 `checkLiked` API 获取点赞状态，`isLiked` 不再硬编码为 `false` |

### 说明
- `resource.js` 中 `checkLiked` 函数已存在但未被使用，现在已接入
- 刷新页面后点赞状态不再丢失

---

## #13 帖子详情页

**状态：✅ 已完成**

### 改动文件

| 文件 | 改动 |
|------|------|
| `entry/.../pages/PostDetailPage.ets` | **新建** — 帖子详情页，包含作者信息、标题、正文、图片、附件、标签、点赞/收藏交互栏（带弹性动画） |
| `entry/.../pages/CommunityPage.ets` | `PostCard` 根 Column 添加 `.onClick()` 跳转到 `PostDetailPage` |
| `entry/.../resources/base/profile/main_pages.json` | 路由列表添加 `"pages/PostDetailPage"` |

---

## #14 通知列表加触发者信息

**状态：✅ 已完成**

### 改动文件

| 文件 | 改动 |
|------|------|
| `neushare-backend/.../vo/NotificationVO.java` | **新建** — 包含通知全部字段 + 触发者信息 `fromUsername`/`fromNickname`/`fromAvatarUrl` |
| `neushare-backend/.../mapper/NotificationMapper.java` | 增加 `IPage<NotificationVO> selectNotificationPageWithSender(...)` 方法 |
| `neushare-backend/.../resources/mapper/NotificationMapper.xml` | 增加 `NotificationVOMap` resultMap 和 `selectNotificationPageWithSender` SQL（LEFT JOIN user 获取触发者信息） |
| `neushare-backend/.../service/NotificationService.java` | `getUserNotifications` 返回类型从 `IPage<Notification>` 改为 `IPage<NotificationVO>` |
| `neushare-backend/.../service/impl/NotificationServiceImpl.java` | `getUserNotifications` 实现改用 `selectNotificationPageWithSender`，返回 `IPage<NotificationVO>` |
| `neushare-backend/.../controller/NotificationController.java` | `/list` 接口返回类型从 `PageResult<Notification>` 改为 `PageResult<NotificationVO>` |

### 说明
- 之前通知只返回 `fromUserId`，前端无法展示"谁触发了这条通知"
- 现在 LEFT JOIN user 表，返回触发者的用户名、昵称、头像

---

## 文件统计

| 类别 | 数量 |
|------|------|
| 后端修改 | 12 个 |
| 后端新建 | 15 个（Post/PostLike/PostFavorite/PostComment 实体 + PostVO/PostCommentVO + 4 Mapper接口 + 4 Mapper XML + PostService/PostCommentService + PostServiceImpl/PostCommentServiceImpl + PostController/PostCommentController + NotificationVO） |
| 鸿蒙端修改 | 7 个 |
| 鸿蒙端新建 | 4 个（PostApi.ets, UserApi.ets, FollowListPage.ets, PostDetailPage.ets） |
| Web 端修改 | 1 个 |
| **总计** | **39 个文件** |
